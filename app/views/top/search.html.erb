<div class="container">
  <h2>Search Result</h2>
  <table class="row table">
    <% @packages.each do |package| %>
      <tr>
        <td class="span4"><%= package.name %></td>
        <td class="span6"><%= package.description %></td>
        <td class="span2"><%= package.author %></td>
      </tr>
    <% end %>
  </table>
  <%= paginate @packages %>
</div>
